import { router } from '../trpc-instance'

export const stripePaymentRouter = router({
  // Removed direct Stripe integration in favor of API routes
  // Kept as placeholder for future Stripe-related business logic
})
